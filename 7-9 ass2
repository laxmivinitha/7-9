#include<stdio.h>
#include<stdlib.h>

int main()
{
	int *p,sum=0,n,i;
    scanf("%d",&n);
	p=(int *)malloc(n*sizeof(int));

	for(i=0;i<n;i++)
	scanf("%d",p+i);
	
	for(i=0;i<n;i++)
	{
	sum=sum+*(p+i);	
	}
	printf("%d",sum);
	return 0;
}
